To setup and run the application, follow these steps:\n1. Create a new virtual environment using python -m venv venv\n2. Activate the virtual environment using source venv/bin/activate\n3. Install the dependencies using pip install -r requirements.txt\n4. Run the application using uvicorn main:app --host 0.0.0.0 --port 8000\n\nTo test the application, use the following command:\npytest tests\n\nExample usage:\n1. Send a POST request to http://localhost:8000/chat with the following JSON body:\n{\"message\": \"Hello\"}\n2. The application will respond with a JSON object containing the chat response.